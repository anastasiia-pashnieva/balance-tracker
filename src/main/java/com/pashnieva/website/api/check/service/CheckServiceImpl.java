package com.pashnieva.website.api.check.service;

import org.springframework.stereotype.Service;

@Service
public class CheckServiceImpl /*implements CheckService*/ {

    /*@Autowired
    private CheckRepository checkRepository;

    @Autowired
    private CommerceItemRepository commerceItemRepository;

    @Override
    public Check getCheck(Long id) {
        return checkRepository.getOne(id);
    }

    @Override
    public List<Check> getAllChecks() {
        return checkRepository.findAll();
    }

    @Override
    public List<Check> getChecksByUser(User user) {
        return checkRepository.findByUser(user);
    }

    @Override
    @Transactional
    public Check createCheck(Check check) {
        return checkRepository.save(check);
    }

    @Override
    @Transactional
    public Check updateCheck(Check check) {
        return checkRepository.save(check);
    }

    @Override
    @Transactional
    public void deleteCheck(Long id) {
        checkRepository.deleteById(id);
    }*/
}
